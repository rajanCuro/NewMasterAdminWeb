import React, { useEffect } from 'react'
import { useAuth } from '../auth/AuthContext';

function AllHospital({data}) {
   
    return (
        <div>AllHospital</div>
    )
}

export default AllHospital